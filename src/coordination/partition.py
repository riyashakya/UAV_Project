"""Area partitioning — Phase 5.

Single responsibility: divide the AOI into N sectors behind one interface. Two strategies:
``grid`` (naive baseline) and ``weighted_voronoi`` (seeded by prior hazard probability, so
sectors are equal-*workload*, not equal-*area*).

Stub: implemented in Phase 5.
"""

from __future__ import annotations


def partition(*args: object, **kwargs: object) -> object:
    """Partition the AOI into N sectors. Stub (Phase 5)."""
    raise NotImplementedError("Phase 5: partitioning is not implemented yet.")
